# V1.0 Protection Policy V1.1

Files whose paths or names contain `V1.0`, `FINAL`, `LOCKED`, `MASTER`, `APPROVED`, or `EXACT` are immutable baseline assets. The manifest records their SHA-256 values. Any mismatch fails verification and CI.
